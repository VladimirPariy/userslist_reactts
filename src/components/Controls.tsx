import React, {Dispatch, FC, SetStateAction} from "react";import Button from "./UI/Button";import styles from '../styles/Controls.module.scss'import {MdOutlineKeyboardArrowLeft, MdOutlineKeyboardArrowRight} from "react-icons/md";interface Props {  pageCount: number;  totalPage: number;  setPageCount: Dispatch<SetStateAction<number>>;}const Controls: FC<Props> = (props) => {  const onPreviousPageHandler = () => {    if (props.pageCount !== 0 && props.totalPage > 0) {      props.setPageCount(prev => prev - 1)      return    }    props.setPageCount(props.totalPage)  }  const onNextPageHandler = () => {    if (props.pageCount !== props.totalPage && props.totalPage >= 0) {      props.setPageCount(prev => prev + 1)      return    }    props.setPageCount(0)  }  console.log(props.totalPage, props.pageCount)  return (    <section className={styles.controlWrapper}>      <Button clickHandler={onPreviousPageHandler}>        <MdOutlineKeyboardArrowLeft />        Previous      </Button>      <Button clickHandler={onNextPageHandler}>        Next        <MdOutlineKeyboardArrowRight size={70}/>      </Button>    </section>  );};export default Controls;