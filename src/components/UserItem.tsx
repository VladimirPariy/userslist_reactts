import React, {Dispatch, FC, SetStateAction} from "react";import styles from '../styles/UserItem.module.scss'import {firstPhone, secondPhone} from "../utils/utils";interface Props {  id: string;  name: string;  email: string;  phone: string;  clickHandler: (id: string) => void;  setVisiblePosts:Dispatch<SetStateAction<boolean>>;}const UserItem: FC<Props> = (props) => {  return (    <div className={styles.userWrap}>      <div className={styles.info}>        <div>{props.name}</div>        <div>{props.email}</div>        <div>{firstPhone(props.phone)}</div>        <div>{secondPhone(props.phone)}</div>      </div>      <button onClick={() => {        props.clickHandler(props.id)        props.setVisiblePosts(true)      }}>        Show posts      </button>    </div>  );};export default UserItem;