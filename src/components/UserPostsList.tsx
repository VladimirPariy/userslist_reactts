import React, {Dispatch, FC, SetStateAction} from "react";import {IPost} from "../types/IPost";import styles from '../styles/PostList.module.scss'import UserPost from "./UserPost";import Error from "./UI/Error";import Loading from "./UI/Loading";interface Props {  posts: IPost[];  postsError: string | null;  isPostsLoading: boolean;  setVisiblePosts: Dispatch<SetStateAction<boolean>>}const UserPostsList: FC<Props> = (props) => {  return (    <div className={styles.allPostsWrapper}>      <div className={styles.postsContainer}>        {props.posts.map(post =>          <UserPost key={post.id} post={post}/>        )}      </div>      {props.postsError && <Error children={props.postsError}/>}      {props.isPostsLoading && <Loading/>}      <button onClick={() => props.setVisiblePosts(false)}              className={styles.hidePostBtn}>        Hide posts      </button>    </div>  );};export default UserPostsList;