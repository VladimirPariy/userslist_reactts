import React, {Dispatch, FC, SetStateAction} from "react";import styles from "../styles/Filter.module.scss";import Input from "./UI/Input";import Button from "./UI/Button";interface Props {  selectedSort: string;  searchUser: string;  setSelectedSort: (sort: string) => void;  setSearchUser: Dispatch<SetStateAction<string>>;}const Filters: FC<Props> = (props) => {  const switchingTypeSort = () => {    if (props.selectedSort === 'id') {      props.setSelectedSort('name')      return    }    props.setSelectedSort('id')  }  return (    <div className={styles.filterWrapper}>      <div className={styles.filterContainer}>        <Input value={props.searchUser}               onChangeHandler={props.setSearchUser}               className={styles.searchInput}/>        <Button clickHandler={() => switchingTypeSort()}>          Switching for sorting by {props.selectedSort === 'id' ? 'name' : 'id'}        </Button>      </div>    </div>  );};export default Filters;